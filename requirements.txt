# requirements.txt
numpy>=1.24
pandas>=2.0
requests>=2.31
pyarrow>=12.0         # parquet cache
matplotlib>=3.7
scipy>=1.11
# ta-lib (optional, fallback built-in nếu không cài)
# talib-binary (pip install TA-Lib) — chỉ cần nếu muốn dùng SAR chính xác hơn
